# RAG 文档索引

这个目录专门收口当前项目中与 RAG 主链路相关的文档。

目标很明确：

1. 技术方案单独讲清楚。
2. 运行流程单独讲清楚。
3. 问题闭环单独讲清楚。
4. 答辩表达单独讲清楚。

这样后面不管是自己维护、复试答辩、还是继续接着改，都不会翻一份混杂文档。

## 1. 当前文档分工

### 1.1 技术总览

- `RAG与Rerank方案说明.md`

负责：

1. 主链路架构
2. 模块职责
3. 模型选型
4. 打分逻辑
5. 技术边界

### 1.2 流程与联调

- `RAG主链路执行流程与联调手册.md`

负责：

1. 请求执行顺序
2. 启动顺序
3. 自检与探针
4. 日志与排障顺序
5. 联调操作
6. 实时工作日志

### 1.3 问题闭环

- `RAG主链路优化与问题闭环报告.md`

负责：

1. 真实失败模式
2. 根因判断
3. 修正策略
4. 演进过程
5. 通用规则总结

### 1.4 答辩讲稿

- `RAG答辩讲稿与技术拆解.md`

负责：

1. 答辩开场怎么讲
2. 主链路怎么逐步展开
3. 核心算法到底提升了哪一段
4. 公式和模块怎么解释
5. 常见追问怎么回答

## 2. 当前建议阅读顺序

如果是第一次看，建议按下面顺序：

1. `RAG与Rerank方案说明.md`
   先知道系统现在长什么样。
2. `RAG主链路执行流程与联调手册.md`
   再知道它怎么跑、怎么验。
3. `RAG主链路优化与问题闭环报告.md`
   再看它为什么会长成现在这样。
4. `RAG答辩讲稿与技术拆解.md`
   准备答辩时最后看，负责把前面几份材料收束成可讲述口径。

## 3. 这个目录当前已经覆盖哪些内容

如果按“细节 / 流程 / 技术”三个维度看，现在这个目录已经分别覆盖了：

### 3.1 技术

1. 主链路架构
2. hybrid 检索
3. rerank
4. `QueryIntentBuilder -> UnifiedEvidenceScorer -> AnswerModeRouter` 三段式收口
5. 选择性 `LLM Judge`
6. 抽取式 / 结构化 / 生成式回答

### 3.2 流程

1. 启动顺序
2. 请求执行顺序
3. 健康检查
4. 单测
5. probe
6. 页面实测
7. 日志排查
8. 实时工作日志
9. 答辩口径

### 3.3 细节

1. 模块职责
2. 模型选型
3. 关键配置
4. 失败案例
5. 修复边界
6. 现有残留问题
7. 答辩讲稿

## 4. 相关实验材料

RAG 目录里放的是系统落地文档。

如果你要看离线 benchmark、消融实验和复试材料，请继续看：

1. `../../experiments/主流RAG检索对比实验/方法改进与结果解读.md`
2. `../../experiments/主流RAG检索对比实验/详细流程与方法说明.md`
3. `../../experiments/消融实验_模块贡献分析/消融实验说明.md`

## 5. 当前主线结论

当前项目的 RAG 已经不再是“单 query 向量召回 + LLM 自由回答”的松散流程，而是：

```text
query 去噪 / 关键词视图
-> QueryIntentBuilder
-> 单路向量召回 + 文档级 BM25
-> 自适应 hybrid 融合
-> cross-encoder rerank
-> UnifiedEvidenceScorer
-> AnswerModeRouter
-> Extractive / Structured / Generative
-> sources 展示
```

其中：

1. 当前默认主链路强调“意图路由 + 自适应 hybrid + 统一证据评分 + 模式路由”，不再使用旧的 `AgentService + universal defense` 叠加式结构。
2. `LLM as a Judge` 是选择性启用，当前更常覆盖短实体、地点和 `X 的 Y` 属性型高风险 query。
3. 当前运行面不再引入额外的历史检索增强模块或额外生成后审核层。

这条链路当前最核心的目标不是“尽量多答”，而是：

1. 召回更稳。
2. 引用更准。
3. 回答边界更清楚。
4. 该拒答时就拒答。

当前默认口径也已经固定：

1. `adaptive hybrid + rerank + UnifiedEvidenceScorer` 是当前算法主线。
2. `AnswerModeRouter + GeneratorFactory` 是当前回答分流主线。
3. `LLM as a Judge` 只在高风险 query 上启用，当前由 `needs_judge = defense_profile is STRICT` 决定。
4. `CoT` 没有外显成长链思维，当前只保留为结构化证据判别思路。
5. 当前答辩和联调都只围绕真实在线主链路展开，不再把历史增强方案写成现有能力。
