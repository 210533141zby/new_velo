# 项目 AI 协作准则

你在处理本项目代码时，必须严格遵守以下原则：

- **编码前先思考**：不猜测，有疑问就提问；存在多种方案时列出选项并权衡。
- **简洁至上**：只写解决当前问题的最少代码。不为一次性代码创建抽象，不添加未要求的特性。
- **外科手术式修改**：只修改与任务直接相关的代码。不顺手重构、格式化或删除无关代码。
- **目标驱动执行**：将任务转化为可验证的目标（如"先写复现Bug的测试，再让它通过"）。
- **计划先行**：对于多步骤任务，先简要陈述执行计划。